---
- name: Configure AdGuard metrics identity
  hosts: standby
  gather_facts: true
  become: true
  roles:
    - role: adguard_metrics_user

- name: Collect AdGuard operational metrics
  hosts: platform
  gather_facts: true
  become: true
  roles:
    - role: adguard_metrics_collector
